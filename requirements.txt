# Pisos de versão em vez de pins exatos: os pins antigos (fastapi 0.111 /
# pydantic 2.7) não têm wheel para Python 3.12+ e falham ao compilar no Windows.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0.30
jinja2>=3.1.4
python-multipart>=0.0.9
python-dotenv>=1.0.1
itsdangerous>=2.2.0
pydantic>=2.9
pydantic-settings>=2.5
alembic>=1.14
psycopg[binary]>=3.2
tzdata>=2024.1; sys_platform == "win32"
# Integração com Active Directory (puro Python e compatível com Windows/Linux).
ldap3>=2.9.1
# Opcional: driver PostgreSQL (descomente se for usar Postgres ao invés de SQLite)
# psycopg2-binary==2.9.9
